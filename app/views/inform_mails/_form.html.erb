<%= form_for(@inform_mail) do |f| %>
  <% if @inform_mail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inform_mail.errors.count, "error") %> prohibited this inform_mail from being saved:</h2>

      <ul>
      <% @inform_mail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
