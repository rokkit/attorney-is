<%= simple_form_for(@inform_mail) do |f| %>
  <% if @inform_mail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inform_mail.errors.count, "error") %> prohibited this inform_mail from being saved:</h2>

      <ul>
      <% @inform_mail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field advocate-message">
    <%= f.association :user, label: "Получатель"  %>
  </div>
  <div class="field email-message">
    <%= f.input :body, label: 'Соообщение' %>
  </div>
  <div class="field">
    <%= f.input :sms, label: 'СМС-сообщение' %>
  </div>
  <div class="field">
    <%= f.input :email, label: 'Эл. почта' %>
  </div>
  <div class="actions">
    <%= f.submit "Отправить" %>
  </div>
<% end %>
