<%= simple_form_for(@inform_mail) do |f| %>
  <% if @inform_mail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inform_mail.errors.count, "error") %> prohibited this inform_mail from being saved:</h2>

      <ul>
      <% @inform_mail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.association :user, label: "Адвокат", input_html: {style:'width:400px;'}  %>
  </div>
  <div class="field">
    <%= f.input :body, label: 'Соообщение', input_html: {style:'width:400px;'} %>
  </div>
  <div class="field">
    <%= f.input :sms_body, label: 'СМС-сообщение', input_html: { style:'width:400px;'} %>
  </div>
  <div class="actions">
    <%= f.submit "Отправить" %>
  </div>
<% end %>
