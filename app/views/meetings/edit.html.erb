<h1>Изменить информацию о заседании</h1>

<%= render 'form', locals: { :edit => true } %>
<%= link_to link_to raw("<i class='icon-chevron-left'></i> Назад"), domain_meetings_path(@meeting.domain), class: 'actions' %>
