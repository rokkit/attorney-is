<%= form_for(@meeting) do |f| %>
  <% if @meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
      <% @meeting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Тема" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content, "Дополнительная информация" %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :will_be_on, "Дата" %><br />
    <%= f.text_field :will_be_on %>
  </div>
  <div class="field">
    <%= f.label :will_be_at, "Время" %><br />
    <%= f.text_field :will_be_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
