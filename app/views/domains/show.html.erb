
<p>
  <h3><%= @domain.try :name %></h3>
  </p>
<div class="row">
  <div class="well span4"><%= link_to raw("<i class='icon-calendar'></i> Судебные заседания"), domain_meetings_path(@domain) %></div>
  <div class="well span4"><%= link_to raw("<i class='icon-user'></i> Адвокаты"), users_path(dom: @domain) %></div>
</div>
<div class="row">
  <div class="well span4"><%= link_to raw("<i class='icon-question-sign'></i> Заявки на заседания"), request_meetings_path(dom: @domain) %></div>
</div>

<%= link_to 'Редактировать', edit_domain_path(@domain) %> |
<%= link_to 'Назад', domains_path %>
